This directory will hold the final result of segmentation of the images
contained in the "original" directory.

Each image will be cropped to the minimum bounding box of the segmented sample
and will only contain the pixels within the segmentation mask.
